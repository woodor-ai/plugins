{
  "description": "Meeting-room lifecycle: ensure data dir + venv, auto-launch daemon on host machine, inject context on SessionStart.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/bin/session-bootstrap.py\""
          }
        ]
      }
    ]
  }
}
